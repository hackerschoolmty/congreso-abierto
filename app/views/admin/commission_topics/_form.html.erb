<%= form_for @commission_topic, url: admin_commission_topics_path(@commission_topic) do |f| %>
  <% if @commission_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@commission_topic.errors.count, "error") %> prohibited this commission topic from being saved:</h2>

      <ul>
      <% @commission_topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>